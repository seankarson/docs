<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "http://docs.oasis-open.org/dita/v1.1/OS/dtd/concept.dtd">
<concept
	id="accessing-entries">
	<title>Accessing Entries</title>
	<conbody>
		<section
			id="section_310C0BE952BC489C9E97811DAE8736E4">
			<p>Use the API to retrieve the entry key, entry value, and the
					<codeph>RegionEntry</codeph> object itself. A variety of functions provide
				information for individual entries and for the set of all entries resident in the
				region. The online API documentation lists all available access functions. </p>
			<p>A region's entry keys and <codeph>RegionEntry</codeph> objects are directly available
				from the local cache. Applications can directly access the local cache's stored
				entry value through the <codeph>RegionEntry::getValue</codeph> function. The
					<codeph>getValue</codeph> function either returns the value if a valid value is
				present in the local cache, or <codeph>NULL</codeph> if the value is not valid
				locally. This function does no data loading, nor does it look elsewhere in the
				distributed system for a valid value. </p>
				<note>Direct access through <codeph>RegionEntry::getValue</codeph> does not reset an entry's
				timestamp for LRU expiration. See <xref
					href="expiration-attributes.xml#expiration-attributes" type="concept"
					format="dita" scope="local"
					><?xm-replace_text Specifying Expiration Attributes?></xref> for more
				information about LRU expiration.</note>
			<p>In comparison, the standard <codeph>Region::get</codeph> functions consider all
				caches and all applicable loaders in the distributed system in an attempt to return
				a valid entry value to the calling application. The primary attribute setting
				affecting entry retrieval is <codeph>CacheLoader</codeph>. See <xref
					href="application-plugins.xml#application-plugins"
					type="concept"
					format="dita"
					scope="local"><?xm-replace_text Specifying Application Plug-In Attributes?></xref>. </p>
			<p>The standard <codeph>Region::get</codeph> functions may implement a number of
				operations in order to retrieve a valid entry value. The operations used depend on
				the region's attribute settings and on the state of the entry itself. By default,
				the client retrieves entry values through calls to the <codeph>get</codeph>
				function. The client can override this behavior for any region by defining a cache
				loader for the region. </p>
			<p>The following sections discuss the <codeph>get</codeph> function and special
				considerations for entry retrieval. </p>
		</section>
		<section
			id="section_AD6AFD842C144C128FA1C7F0B9283372">
			<title>Entry Retrieval</title>
			<p>You retrieve entry values with the <codeph>Region::get</codeph> function. </p>
			<p>When an entry value is requested from a region, it is either retrieved from the cache server
				or fetched by the region's locally-defined cache loader in this sequence: </p>
			<ol
				id="ol_1A9CCF9F75C84D1BB01BB68C0CAFBFEB">
				<li
					id="li_C7BC1A0666DB4F44A459080CF1D18FB2">local cache search </li>
				<li
					id="li_0DCC3E87C06A440499DE53613F464865">server cache </li>
				<li
					id="li_BDDC2BCA24854985ACE4AEC2B8CCC100">local load (For distributed regions,
					the local load is fetched before remote cache values) </li>
			</ol>
		</section>
		<section
			id="section_82B624FE364F4034AF010D20A8D0DF68">
			<title>How the get Operation Affects the Local Entry Value</title>
			<p>If a <codeph>get</codeph> operation retrieves an entry value from outside the local
				cache through a local load, it automatically <codeph>put</codeph>s the value into
				the cache for future reference. </p>
			<p>Note that these load operations do not invoke a cache writer. Because the loader and
				writer operate against the same data source, you do not need to perform a cache
				write for entries that were just fetched from that data source. For descriptions of
				these processes, see <xref
					href="application-plugins.xml#application-plugins"
					type="concept"
					format="dita"
					scope="local"><?xm-replace_text Specifying Application Plug-In Attributes?></xref>. </p>
				<note>Access through a <codeph>get</codeph> operation resets an entry's timestamp for LRU
				expiration.</note>
		</section>
		<section
			id="section_F636614605C84513B2B7F4A674FD6E38">
			<title>Getting Multiple Entries Using getAll</title>
			<p>You can use the <codeph>getAll</codeph> Region API to get all values for an array of
				keys from the local cache or cache server. Under <xref
					scope="local"
					href="../cpp-caching-api/add-entry-to-cache.xml#concept_26D4E6C6BC6F4AB8884E33119999656D/section_748916759F0246619CD27E7456DCA365"
					type="section"
					format="dita">Bulk Put Operations Using putAll</xref> for more information. </p>
		</section>
	</conbody>
</concept>
